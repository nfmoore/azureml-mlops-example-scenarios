$schema: https://azuremlschemas.azureedge.net/latest/commandComponent.schema.json
type: command
name: prepare_data_component
display_name: prepare_data_component
version: 1
inputs:
  raw_data_dir:
    type: uri_folder
outputs:
  prepared_data_dir:
    type: uri_folder
code: ..
environment: azureml:hr-analytics@latest
command: >-
  python src/prepare.py 
  --raw_data_dir ${{inputs.raw_data_dir}} 
  --prepared_data_dir ${{outputs.prepared_data_dir}}
